你是一位資深前端工程師。

使用以下技術產生一個響應式儀表板佈局：

- Bootstrap 5
- Vanilla JavaScript（ES6）
- Vite 環境（不使用 React 或 Vue 等框架）

依照以下需求，設計一個船隊決策支援儀表板：

## 核心目的

儀表板協助船隊操作人員識別未來 24 小時內可能具有風險的船隻。

## UI 風格

- 極簡主義
- 深色主題
- 工業 / 監控系統風格
- 高資訊密度但版面整潔

## 版面結構

使用 Bootstrap 12 欄網格系統。

主要版面：

- 左側側欄（col-2）：船隻清單
- 中央區域（col-7）：地圖檢視（主要視覺焦點）
- 右側面板（col-3）：
  - 上方：事件 / 警示清單
  - 下方：船隻詳細資訊面板

頂部區域：

- KPI 摘要列（例如：總船數、延誤船數）

選配底部：

- 時間軸回放控制列

## 必要元件

### 1. 船隻清單（左側側欄）

- 可滾動
- 點擊以選取船隻
- 醒目顯示已選取的船隻

### 2. 地圖檢視（中央）

- 佔位容器（不需要真實地圖 API）
- 顯示船隻標記（模擬資料）
- 滑鼠懸停提示框：
  - 船隻名稱
  - 預計抵達時間（ETA）
  - 延誤狀態

### 3. 事件 / 警示面板（右上）

- 事件清單
- 顯示類型（延誤、異常）
- 顯示嚴重程度

### 4. 船隻詳細資訊面板（右下）

- 顯示已選取船隻的詳細資料：
  - 速度
  - 實際 ETA 與預期 ETA 比較
  - 狀態標記（延誤、異常、偏航）

### 5. KPI 列（頂部）

- 總船數
- 延誤船隻數量

### 6. 時間軸回放（選配）

- 控制項以模擬船隻歷史軌跡回放

## 互動需求

- 點擊清單中的船隻，同步更新地圖與詳細資訊面板
- 滑鼠懸停於地圖標記上顯示提示框
- 同一時間只能選取一艘船隻

## 資料（模擬）

使用如下的模擬 JSON 結構：

{
ships: [
{
id, name, lat, lng,
speed,
eta,
expectedEta,
status: {
delay,
speedAnomaly,
routeDeviation
}
}
],
events: [
{
shipId,
type,
severity,
timestamp
}
]
}

## 技術限制

- 僅使用 Bootstrap 類別（不使用其他 CSS 框架）
- 模組化 JavaScript 結構（元件分離）
- 程式碼簡潔且易於維護
- 避免行內 JS

## 輸出格式

- 需提供：
  1. index.html
  2. main.js
  3. 以元件為基礎的 JS 結構
  4. 最精簡的 CSS（深色主題）

重點在於版面整潔，並具備未來擴充風險評分系統的可延伸性。
